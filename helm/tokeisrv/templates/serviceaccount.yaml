{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ default (include "tokeisrv.name" .) .Values.serviceAccount.name }}
  labels:
    {{- include "tokeisrv.labels" . | nindent 4 }}
{{- end }}
